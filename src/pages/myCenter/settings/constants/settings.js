export const Actions = {
    SETTINGS: "settings_save",
    SETDATA: "settings_data",
    DEL_LOGIN:"del_login"
};

export const initalzeState = {
    enableMessage: true
};

export const reKey = "settings";
